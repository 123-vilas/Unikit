<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.unikit" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="33"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/unikitsplash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/unikitsplash"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniKit"
        tools:targetApi="33" >

        <activity
            android:name=".DateCounter"
            android:exported="true" />
        <activity
            android:name=".temp_cal"
            android:exported="true" />
        <activity
            android:name=".scientificcalculator"
            android:exported="true" />
        <activity
            android:name=".Translator"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>